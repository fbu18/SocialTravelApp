<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item_trophy_layout"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:layout_marginBottom="10dp"
    android:clickable="false"
    android:focusable="false"
    android:focusableInTouchMode="false">

    <TextView
        android:id="@+id/tvTrophyName"
        android:layout_width="75dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/tvTrophyDescription"
        android:text="Locked"
        android:textColor="@color/colorPrimaryDark"
        android:textAlignment="center"
        android:textSize="16dp" />

    <ImageView
        android:id="@+id/ivTrophy"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/tvTrophyName"
        android:layout_marginStart="15dp"
        app:srcCompat="@drawable/emoji_questionmark" />

    <TextView
        android:id="@+id/tvTrophyDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/ivTrophy"
        android:layout_below="@+id/ivTrophy"
        android:text="Explore to unlock!"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="10sp" />

    <View
        android:id="@+id/profile_divider"
        android:layout_width="fill_parent"
        android:layout_height="2dip"
        android:layout_below="@+id/tvTrophyDescription"
        android:layout_marginTop="15dp"
        android:background="@drawable/background_gradient" />

</RelativeLayout>


